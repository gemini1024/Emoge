package com.emoge.app.emoge.ui.frame;

/**
 * Created by jh on 17. 8. 16.
 * frame 클릭했을때 Listener.
 * swap 가능하므로 id를 인자로 받음.
 */

interface OnFrameClickListener {
    void onFrameClick(long frameId);
}
